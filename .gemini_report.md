The linting issues in `tests/IntegrationTests/ServiceTests.cs` have been resolved.

**Changes:**
1.  **Removed Unused Usings:** Removed unnecessary `using` directives (`Aiursoft.EmployeeCenter.Authorization`, `Aiursoft.EmployeeCenter.Entities`, etc.) that are already covered by `tests/GlobalUsings.cs` or unused.
2.  **Fixed Assertion:** Replaced `Assert.IsTrue(content.Contains("Services"))` with `Assert.Contains("Services", content)` to comply with the linter's recommendation and project patterns (verified against `BasicTests.cs`).
3.  **Compilation Verified:** `dotnet build tests/Aiursoft.EmployeeCenter.Tests.csproj` passed successfully.
4.  **Version Check:** Verified that `Aiursoft.EmployeeCenter.Entities` version was already bumped to `1.0.57` in the previous commit, so no further bump is needed.

The changes are committed to the `fix-issue-93` branch.